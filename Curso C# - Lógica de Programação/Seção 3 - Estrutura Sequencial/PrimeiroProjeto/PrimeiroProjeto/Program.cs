using System; // o 'using' está mais claro que os outros termos em azul porque ele é desnecessário no código, pois já está importado no código, não precisando importá-lo explicitamente.

namespace PrimeiroProjeto // Note: actual namespace depends on the project name.
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}